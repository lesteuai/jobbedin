app/api/settings/route.ts:
- See if there is a way to check for existence of apiKey instead of pulling it. Within one query if possible.
- openrouterApiKey in the insert phase should not exist if
+ there is no change to the api Key
+ only null if it is asked to clear key. right now, saving settings will reset openrouterApiKey to null
app/lib/openrouter.ts:
- decrypt api key in here instead of sending key from outside to create LLM

app/api/jobs/[id]/analyze/route.ts:
- If jobs failed, they don't restart the right way. They just created new jobs with the same resume+job. There should be a better way to handle this (like restart will delete old jobs if they all failed)

app/resumes/page.tsx:
- Sometimes when coming back from another page (e.g. settings) and press "To job" in Resumes Screen, it goes to Jobs but the first job is highlighted with no content on the right side, but can be analyzed because it's the correct jobId.

app/lib/app-store.tsx:
http://localhost:3000/resumes/8ffc4aa9-9f35-4480-9b90-26c95b421f37 Bug no resume when I access one instance of resume without selecting in the app itself

app/lib/workflow.ts:
- Sometimes, words generated by LLM become gibberish. It can be in any tab, mostly in run_ResearchCompany, run_CrossRef.

app/lib/components/AnalysisReport.tsx:
- All tabs that failed should show reason.
- Generate tabs should be like the other tabs, do not show chatpanel and show reason of failure

app/lib/openrouter.ts:
- add an extra exception to catch error code 401 with error.constructor.name='AuthenticationError'

general:
- "OpenRouter is out of credit. Add your own OpenRouter API key in Settings, then re-analyze." => "Free trial is over. Add your own OpenRouter ..." and move to a shared message
- out_of_credit move to a shared variable
